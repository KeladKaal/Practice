pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'docker run test/cowsay-dockerfile "Moo"'
            }
        }
    }
}
        
